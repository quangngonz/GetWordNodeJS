# Cách dùng

## Tải thư viện:

```
npm install
```

## Start server:

```
npm start
```

## Cách lấy từ trong python:

```
import requests

word = requests.get('http://localhost:3000').text

print(word)
```
